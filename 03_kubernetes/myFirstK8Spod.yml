# 파일경로(윈도우) | C:\Users\사용자명\Documents\kube_folder
# 파일경로(리눅스) | /home/사용자명/kube_folder

apiVersion: v1
kind: Pod
metadata:
  name: myFirstK8Spod # pod name
  labels:
    app: myFirstK8Skube
spec:
  containers:
  - name: myFirstK8S_ex91 #container name
    image: httpd
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
